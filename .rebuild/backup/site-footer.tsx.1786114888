export default function SiteFooter() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto flex max-w-6xl flex-col gap-2 px-6 py-8 sm:flex-row sm:items-center sm:justify-between">
        <p className="font-mono text-xs uppercase tracking-widest text-muted">
          ETERNA APPLICATION // eterna.vstal.in
        </p>
        <p className="text-sm text-muted">
          © {new Date().getFullYear()} Vstalin — application for Lead Automation &amp; Web Engineer at Eterna Indonesia
        </p>
      </div>
    </footer>
  );
}
