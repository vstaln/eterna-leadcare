# Pipeline connectivity (filled in the pipeline build phase; never commit real values)
N8N_BASE_URL=
N8N_API_KEY=
# Shared secret for the inbound lead webhook
WEBHOOK_TOKEN=
# Bearer token for the /api/execution status endpoint
EXECUTIONS_AUTH_TOKEN=
N8N_WEBHOOK_PATH=
