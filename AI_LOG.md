# AI_LOG.md — Cursor / OpenCode session log

Append-only. One entry per work block (~2-4h). Format: goal → real prompt snippet → files changed → verification. Failures and rejections are logged too — the log is the evidence.

## S001 — Repo bootstrap (OpenCode, ~45 min)

- Goal: stand up the project repo + skeleton so CI and git history start clean.
- Prompt: "Create public GitHub repo eterna-ops-command-center for user vstaln, apply branch protection on main (enforce admins, linear history, no force push, no deletions), clone locally, and seed README.md, .gitignore (secrets), and a conventional-commit template."
- Tooling note: repo ops done via GitHub REST API (curl with PAT) because the GitHub MCP server was added to opencode mid-session; MCP will be used from S002 onward.
- Changed: repo vstaln/eterna-ops-command-center (created, protected), README.md, .gitignore, .gitmessage, this file.
- Verified: `git branch --show-current` → main; API returned protection settings (enforce_admins enabled, linear_history enabled, force_pushes/deletions disabled).

## S002 — Scaffold & foundation slice (OpenCode, ~6-8h across a phone-only Termux box)

- Goal: scaffold the Next.js 16 app (Branch B front door), design-token layer, app shell (nav/hero/static pipeline), health endpoint + env stub, CI, docs.
- Prompt: "Execute plan docs/superpowers/plans/2026-08-06-scaffold-foundation.md via subagent-driven development; do single-node tasks inline with a critique review round per task; verify honestly before claiming done."
- Changed: scaffold (package.json, app/, components/, lib/, public/, configs), docs/design-tokens.md, .github/workflows/ci.yml, app/api/health, lib/env.ts, .env.example, README quickstart, ARCHITECTURE.md status, this entry. Branch feat/scaffold-nextjs, 9 commits.
- Adaptations (logged honestly): (1) Turbopack has no native bindings on android/arm64 — build AND dev pinned to `--webpack` (works everywhere, deterministic; CI on ubuntu runs the same). (2) Node CLI shebangs (`#!/usr/bin/env`) fail on this Termux — no /usr/bin; workarounds: explicit `node <realpath>` invocation, LD_PRELOAD=libtermux-exec.so for npm scripts. (3) Scaffold .gitignore `.env*` would have silently dropped .env.example — restored repo's explicit env patterns during review. (4) create-next-app's dev script flagged --turbopack; adapted per plan. (5) Ruling: AGENTS.md kept at repo root (auto-regenerated by `next dev`; committing it prevents a permanently dirty tree) — outside plan §6 allowed paths, kept under controller ruling.
- Verification: `npm run build` 0, `npm run lint` 0, `npx tsc --noEmit` 0 on every task; env prod-throw executed directly (Node 26 native type stripping); CI yaml parsed (js-yaml); prerendered HTML spot-checked for honesty labels ("STATIC PREVIEW", "soon" tags, "SYSTEM: BUILD"). Task reviews: task 1 needed two fix rounds (3 iterations); tasks 2-5 approved on first pass (task 3 with two post-approval nit fixes); final branch review caught the CTA `text-base` color-token regression (restored) + gitignore secrets patterns + 5 nits.

## S003 — pipeline-core: N8N lead pipeline on Oracle box (ET-48) (OpenCode, 2026-08-06)

- Goal: build the real pipeline behind the front door: visitor lead form → hardened Next.js proxy → self-hosted N8N on the Oracle box (`oracle-old`) → RDAP enrichment → Google Apps Script audit trail in Sheets → live report card, with execution records in a gitignored local store.
- Prompt: "Execute plan docs/superpowers/plans/2026-08-06-pipeline-oracle.md via subagent-driven development; do single-node tasks inline with a critique review round per task; verify honestly before claiming done."
- Changed: box `/opt/eterna/{compose.yml,.env}` (outside repo; n8n pinned to 1.123.69, sqlite, `N8N_TASK_RUNNERS_ENABLED=false`, `GENERIC_TIMEZONE=Asia/Jakarta`), docs/n8n-workflow.json ("ET-48 lead pipeline", workflow id `e5336198-9ef1-46e5-8746-4681e17aba1f` — HMAC verify → RDAP enrich → Apps Script log → Respond 200/401, headless import + `--active=true`), docs/apps-script-setup.md (token-gated doGet log endpoint, deploy checklist), lib/store.ts (JSON store, atomic writes, 100-row cap), lib/crypto.ts (HMAC canonical fields + timing-safe verify), lib/env.ts (REQUIRED = 3), app/api/lead (HMAC proxy), app/api/executions (authed) + public mirror, .gitignore (data/), README + ARCHITECTURE scrub, this entry. Branch feat/pipeline-core.
- Adaptations (logged honestly): (1) No Postgres this slice — n8n on default sqlite; execution records in a gitignored JSON file store (`data/executions.json`) — zero exposed DB ports, zero native deps; P5 migrates to Postgres on the box. (2) HMAC over canonical fields (`executionId.nonce.ts`), not raw bodies — no JSON-canonicalization footgun; 5-min freshness; replay within the window is an accepted, documented demo tradeoff. (3) Headless import on the box: `docker exec <c> n8n import:workflow` + `update:workflow --active=true` — no browser needed. (4) Disk audited before compose (started ~88% full, 5.7G free; target ≥3G free after compose) with image prune only. (5) Apps Script logged via a single GET `doGet` endpoint — anonymous "Anyone" deployments reject POST (real Apps Script constraint, documented). (6) Key decision — pinned n8n 1.x (1.123.69): n8n 2.x only publishes workflow versions from the browser/API, which conflicts with phone-only headless CLI management (TRIZ: separation by condition).
- Constraints/honesty: Telegram notify is pending (P3/P5) and never claimed live; `[DEMO]` prefix in Sheets; public endpoints strip PII; secrets never in repo (box `/opt/eterna/.env` + phone `.env.local`, both gitignored). Apps Script is documented but NOT yet deployed — user-gated browser step; n8n TCP 5678 currently bound to localhost — opening the OCI security-list rule is user-gated.
- Verification: live E2E verified on the box (n8n 1.123.69, container `eterna-n8n-1`, workflow `e5336198-9ef1-46e5-8746-4681e17aba1f`): bad signature → 401 `{"ok":false,"error":"bad signature"}`; stale ts → 401 `{"ok":false,"error":"stale timestamp"}`; good signature → 200 degraded `{"ok":false,"error":...}` while `APPS_SCRIPT_URL` is empty on the box (honest degraded path — upgrades to `{"ok":true}` once the user deploys the Apps Script per `docs/apps-script-setup.md`). Final-review round fixed 5 workflow JSON issues found during the E2E round: (1) pure-JS HMAC — `require('crypto')` is blocked in the n8n Code sandbox on this install; (2) webhook-v2 output flattening (`item.body` vs top-level); (3) missing IF `combinator` made the gate always-true; (4) `responseCode` must live in `parameters.options`, top-level was ignored; (5) `onError` is node-level, inside `parameters` it was ignored.

## S004 — ops dashboard + QA + migration docs (ET-48 v2 final slice) (OpenCode, 2026-08-06)

- Goal: ship the final ET-48 v2 slice — an honest ops dashboard (`/ops`) that turns the execution store into a live report card, a route-level QA sweep over the whole branch, and the closing docs story (README pass, Webflow migration plan) so the repo reads as a finished, phone-built showcase.
- Prompt: "Execute plan docs/superpowers/plans/2026-08-06-ops-dashboard.md via subagent-driven development; do single-node tasks inline with a critique review round per task; verify honestly before claiming done."
- Changed: app/ops/page.tsx (server-rendered dashboard: SIGNAL stage strip with per-stage state+source and N/R flags, LEDGER of known unknowns, LOG last-10 strip board with totals + last-failure pin + RENDERED footer, PIPELINE LEGEND; env-presence reads only, never validateEnv() — missing vars degrade to honest PENDING labels, not a 500), components/nav.tsx (Ops link live with aria-current="page"), app/api/lead route.ts (honeypot fix, QA Bug 1), docs/webflow-migration.md (NEW — one-pager template + migration checklist; locked: no Webflow iframe on the Next.js site), README.md story pass, AI_LOG.md this entry. Branch feat/ops-dashboard.
- Constraints/honesty: every state on /ops is env-derived or store-derived, never live-claimed — n8n shows CONFIGURED (presence only; no reachable status endpoint), RDAP shows N/R (no runtime readout from the phone); the N/R-flag honesty metaphor from the concept pass ("unreadable instruments drop their flags, never green lights") is the page's ruling aesthetic; PENDING = user-gated (TCP 5678 exposure via OCI security list, Apps Script deploy per docs/apps-script-setup.md) or P5 (Telegram notify, N8N owner API key, Postgres, public deploy); public surfaces strip PII; `.env.local` untouched; nothing pushed.
- Verification: QA sweep 18/18 behavioral cases + 3/3 static (next build --webpack 0, eslint 0, tsc 0): all public routes 200, /api/lead validation 400s, honeypot 200-decoy with zero store rows, bad-HMAC forward 502 with stub down, /api/executions 401 without bearer, 12-lead stub E2E → id slice + totals line present in rendered /ops HTML, store cap verified at exactly 100 with 107 posts, no-env run renders honest PENDING instead of crashing (run D). One bug fixed: honeypot returned 400 (bot-detectable) — now returns 200 `{"ok":true}` decoy with no store write; build/lint/tsc re-ran 0 after the fix. PII grep 0 across /ops HTML and the public mirror.

## Template

### S00N — <short title> (<tool>, ~<time>)

- Goal: ...
- Prompt: ...
- Changed: ...
- Verified: ...
